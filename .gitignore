# generated during remediation, rebuilt from the seed on every run
data/hardware-inventory-clean.csv

# personal work notes, stays on my machine
WORKLOG.md

# editor and OS junk
.vscode/
.idea/
*.swp
*.tmp
.DS_Store
Thumbs.db

# note: reports/ is tracked on purpose.
# accuracy-log.csv and the audit reports are the evidence artifacts.
# data/hardware-inventory.csv (the seed) is tracked on purpose too.
